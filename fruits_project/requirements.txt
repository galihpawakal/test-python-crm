ultralytics>=8.0.0
opencv-python>=4.6.0
# Note: For GPU support on Windows, install PyTorch separately using:
# pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# (or cu121 depending on your CUDA version)
